services:
  - type: web
    name: benefit-calculator-a2a
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
